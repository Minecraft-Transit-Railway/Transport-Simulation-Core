/**
 * Constants used for map rendering and visualization.
 * See CODE_STYLES.md § 3.1 for more details on constant organization.
 */

/** Spacing between arrows on one-way route indicators (pixels) */
export const ARROW_SPACING = 80;

/** Size of client avatar images on the map (pixels) */
export const CLIENT_IMAGE_SIZE = 32;

/** Padding between client avatars in groups (pixels) */
export const CLIENT_IMAGE_PADDING = 5;

/** Animation duration for route transitions (milliseconds) */
export const ANIMATION_DURATION_MILLIS = 2000;
