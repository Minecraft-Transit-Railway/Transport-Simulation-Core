const SETTINGS = {
	url: `${document.location.origin}${document.location.pathname.replace("index.html", "")}mtr/api/data/`,
	scale: 1,
	maxText: 32,
	blackAndWhite: false,
	routeTypes: ["train_normal"],
};

export default SETTINGS;
