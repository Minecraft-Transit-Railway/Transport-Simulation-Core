/**
 * DTOs and response builder for the {@code /oba/api/where/*} endpoints. Wire shapes follow the {@code OneBusAway} REST API so any OBA client library works against this server unchanged.
 */
@NullMarked
package org.mtr.core.oba;

import org.jspecify.annotations.NullMarked;
