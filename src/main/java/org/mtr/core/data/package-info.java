/**
 * Persisted simulation state and the in-memory data model: stations, platforms, routes, depots, sidings, vehicles, rails, lifts, clients and their loaders. Every class here is non-null by default ({@code @NullMarked}).
 */
@NullMarked
package org.mtr.core.data;

import org.jspecify.annotations.NullMarked;
