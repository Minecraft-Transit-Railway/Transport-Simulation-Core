/**
 * DTOs returned by the {@code /mtr/api/map/*} endpoints &mdash; stations and routes graph, live departures, client positions, and directions request/response payloads consumed by the bundled Angular dashboard.
 */
@NullMarked
package org.mtr.core.map;

import org.jspecify.annotations.NullMarked;
