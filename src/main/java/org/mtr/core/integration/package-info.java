/**
 * Generic {@code Request} / {@code Response} envelope and supporting DTOs shared with embedded hosts (the Minecraft Transit Railway mod) and HTTP callers. The wire format mirrors the schema under {@code buildSrc/src/main/resources/schema/integration/}.
 */
@NullMarked
package org.mtr.core.integration;

import org.jspecify.annotations.NullMarked;
